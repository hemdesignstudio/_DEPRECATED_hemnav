[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pylint = "<2.0.0"
coverage = "*"
pre-commit = "*"

[packages]
zeep = ">=3.4.*"
pyyaml = ">=5.1.*"
python-dotenv = ">=0.*"
doubles = ">=1.5.*"

[requires]
python_version = "3.7"
